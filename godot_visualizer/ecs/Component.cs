namespace Ecs
{
    /// <summary>
    /// The base class for all components in the ECS.
    /// It's an abstract class, meaning it cannot be instantiated on its own.
    /// Components are simple data containers.
    /// </summary>
    public abstract class Component
    {
    }
}